<div align="center">
  <h1>🛡️ Proxy Intelligence Pipeline</h1>
  <p><b>Продвинутый парсер и агрегатор прокси с глубоким мониторингом GitHub, умной фильтрацией и крутым Telegram-ботом.</b></p>
</div>

---

## ✨ Ключевые фичи

- **Глубокий мониторинг GitHub (без лимитов API)**
  - Асинхронная пагинация `search/code` и `search/repositories`.
  - Детальный скан деревьев коммитов (`git tree`) и `blob`-файлов с регулярными выражениями.
  - Умный обход лимитов GitHub API через `raw.githubusercontent.com`.
- **Автономный парсинг и валидация**
  - Подкачка свежих прокси фоновым демоном по заданному расписанию.
  - Быстрая TCP-like валидация на доступность.
  - Умный алгоритм подсчета **Score** (на базе пинга и аптайма).
- **Продвинутый Telegram Админ-Бот**
  - Полное управление пайплайном (старт/стоп/очистка базы).
  - Дашборд со статистикой прямо в чате.
  - Выгрузка живых прокси в формате `CSV` в один клик.
  - Динамическое управление блэклистом стран прямо из интерфейса бота.

---

## 🚀 Быстрый старт

### 1. Подготовка окружения
Убедитесь, что у вас установлен **Python 3.10+**.

```bash
# Активируем виртуальное окружение
python -m venv .venv
.venv\Scripts\Activate.ps1   # (Windows)
source .venv/bin/activate    # (Linux/Mac)

# Устанавливаем зависимости
pip install -r requirements.txt
```

### 2. Настройка (`.env`)
Скопируйте пример файла конфигурации и заполните токены:
```bash
cp .env.example .env
```
Обязательные ключи:
- `TELEGRAM_BOT_TOKEN="ВашТокенОтBotFather"`
- `TELEGRAM_ADMIN_ID=12345678` *(Ваш Telegram ID)*
- `GITHUB_TOKEN="ghp_xxx"` *(Токен для GitHub API, можно без прав)*

---

## 💻 Команды парсера

Управление бота и парсера разделено для стабильности. Все скрипты запускаются через единую точку входа `app.main`.

> 💡 **Совет:** Вы можете запускать разные команды параллельно в разных вкладках терминала! SQLite база поддерживает многопоточную запись.

### 🤖 Telegram Бот (Основной режим)
Запуск панели управления в Телеграм. Из бота можно запускать/останавливать парсер в фоновом режиме, скачивать списки прокси и банить страны.
```bash
python -m app.main run-bot
```

### 🧪 Тестовый запуск (Rate-Limit Safe)
Запускает один цикл сбора прокси, ограничивая поиск до **4 репозиториев**. Идеально для проверки работоспособности `github_token` и `regex` паттернов без риска получить 403 ошибку от GitHub.
```bash
python -m app.main run-once --test
```

### 🔄 Единоразовый полный сбор
Ищет по всем репозиториям и собирает прокси в базу руками (без бота по расписанию).
```bash
python -m app.main run-once
```

### 🕰️ SystemD / Фоновый Демон (Без бота)
Запускает парсер по расписанию (настраивается в `.env` переменной `SCHEDULE_MINUTES`), не включая Telegram-бота.
```bash
python -m app.main daemon
```
